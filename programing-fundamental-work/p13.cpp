#include <iostream>
using namespace std;

main()
{
    int num;
    int sum = 0;
    cout << "Enter 5 numbers:" << endl;
    cin >> num;
    sum = sum + num;
    cin >> num;
    sum = sum + num;
    cin >> num;
    sum = sum + num;
    cin >> num;
    sum = sum + num;
    cin >> num;
    sum = sum + num;
    cout << "Sum = " << sum << endl;
}